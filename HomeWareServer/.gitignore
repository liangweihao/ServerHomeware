# Environment files（仅保留 .env 本地配置，模板见 .env.example）
.env

# IDE
.vscode
.idea

# Python virtual environments
venv/
.venv/

# Python bytecode / cache
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd
.Python

# Log files
logs/
*.log

# Local SQLite DB (dev / prod) — 每个环境的数据库不同，不能同步
*.db
*.db-journal

# Uploads — 用户上传的图片资源，每个环境独立
uploads/

# Exports — 数据导出文件
exports/

# 外部数据目录（数据库 + 上传文件）
../data/
