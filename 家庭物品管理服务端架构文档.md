# 家庭物品管理服务端架构文档

## 1. 项目概述

### 1.1 项目背景

随着生活水平的提高，家庭中购买的物品种类和数量日益增多，如湿巾、卫生纸、药品等日常用品。然而，时间久了，人们常常会面临以下问题：
- 忘记物品的存放位置
- 不清楚物品的剩余数量
- 忽略物品的过期时间
- 缺乏对家庭物品的整体管理

为此，我们设计了一个家庭物品管理系统，旨在帮助用户有效管理家庭物品，并为未来的业务扩展提供基础。本文档详细说明了服务端的架构设计和技术需求。

### 1.2 项目目标

- 开发一个完整的家庭物品管理服务端系统
- 实现用户认证、物品管理、库存管理等核心功能
- 提供RESTful API接口，支持多端接入
- 确保系统的安全性、可靠性和可扩展性
- 为未来的业务扩展（如电商集成、社区功能等）提供基础

## 2. 技术架构

### 2.1 整体架构

```
+----------------+       +----------------+       +----------------+
|                |       |                |       |                |
|    客户端      | <---> |    服务器      | <---> |    数据库      |
|                |       |                |       |                |
+----------------+       +----------------+       +----------------+
```

### 2.2 技术选型

| 分类 | 技术 | 版本 | 选型理由 |
| :--- | :--- | :--- | :--- |
| 服务器语言 | Python | 3.9+ | 简洁易用，生态丰富，拥有强大的Web框架和数据处理库。 |
| 服务器框架 | Django/Flask | 4.2+/2.0+ | Django提供完整的MVC架构和ORM，Flask轻量灵活，根据需求选择。 |
| 数据库 | MySQL | 8.0+ | 成熟稳定的关系型数据库，广泛使用，性能可靠。 |
| 缓存 | Redis | 7.0+ | 用于缓存热点数据和管理用户会话。 |
| 认证 | JWT | - | 无状态认证，便于水平扩展。 |
| API文档 | Swagger/OpenAPI | - | 自动生成API文档，便于前端开发和测试。 |

## 3. 核心模块设计

### 3.1 用户模块

#### 3.1.1 功能需求
- 用户注册/登录
- 个人信息管理
- 家庭管理（创建/加入家庭）
- 权限管理

#### 3.1.2 技术实现
- 基于JWT的认证系统
- 密码加密存储（使用bcrypt等算法）
- 邮箱验证功能
- 家庭邀请机制（生成唯一邀请码）
- 基于角色的权限控制

### 3.2 物品管理模块

#### 3.2.1 功能需求
- 物品添加/编辑/删除
- 物品分类管理
- 物品位置管理
- 物品数量追踪
- 物品过期提醒

#### 3.2.2 技术实现
- RESTful API设计
- 支持物品图片上传（使用云存储或本地存储）
- 实现物品搜索功能（支持模糊搜索）
- 支持批量操作
- 事务处理确保数据一致性

### 3.3 库存管理模块

#### 3.3.1 功能需求
- 库存预警
- 库存报表
- 采购建议

#### 3.3.2 技术实现
- 实现定时任务，定期检查库存状态
- 生成库存分析报表（支持导出）
- 基于历史数据提供采购建议
- 数据可视化支持

### 3.4 数据同步模块

#### 3.4.1 功能需求
- 多设备数据同步
- 离线操作支持

#### 3.4.2 技术实现
- 实现基于时间戳的同步机制
- 支持冲突解决策略
- 提供增量同步API
- 数据变更日志记录

## 4. API设计

### 4.1 认证相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/auth/register` | POST | 用户注册 | `{"username": "...", "email": "...", "password": "..."}` | `{"id": 1, "username": "...", "email": "...", "token": "...", "refresh_token": "..."}` |
| `/api/auth/login` | POST | 用户登录 | `{"email": "...", "password": "..."}` | `{"id": 1, "username": "...", "email": "...", "token": "...", "refresh_token": "..."}` |
| `/api/auth/refresh` | POST | 刷新Token | `{"refresh_token": "..."}` | `{"token": "...", "refresh_token": "..."}` |

### 4.2 用户相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/users/profile` | GET | 获取用户信息 | N/A | `{"id": 1, "username": "...", "email": "..."}` |
| `/api/users/profile` | PUT | 更新用户信息 | `{"username": "..."}` | `{"id": 1, "username": "...", "email": "..."}` |
| `/api/users/password` | PUT | 修改密码 | `{"old_password": "...", "new_password": "..."}` | `{"status": "success"}` |

### 4.3 家庭相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/families` | POST | 创建家庭 | `{"name": "..."}` | `{"id": 1, "name": "...", "invite_code": "...", "created_by": 1}` |
| `/api/families` | GET | 获取用户的家庭列表 | N/A | `[{"id": 1, "name": "...", "role": "admin"}]` |
| `/api/families/{id}` | GET | 获取家庭详情 | N/A | `{"id": 1, "name": "...", "members": [...]}` |
| `/api/families/{id}` | PUT | 更新家庭信息 | `{"name": "..."}` | `{"id": 1, "name": "..."}` |
| `/api/families/{id}` | DELETE | 删除家庭 | N/A | `{"status": "success"}` |
| `/api/families/{id}/join` | POST | 加入家庭 | `{"invite_code": "..."}` | `{"status": "success"}` |
| `/api/families/{id}/members` | GET | 获取家庭成员列表 | N/A | `[{"id": 1, "username": "...", "role": "admin"}]` |
| `/api/families/{id}/members/{user_id}` | PUT | 修改成员角色 | `{"role": "member"}` | `{"status": "success"}` |
| `/api/families/{id}/members/{user_id}` | DELETE | 移除家庭成员 | N/A | `{"status": "success"}` |

### 4.4 物品相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/items` | POST | 添加物品 | `{"name": "...", "category_id": 1, "location_id": 1, "quantity": 10, "unit": "个", "expiry_date": "2024-12-31"}` | `{"id": 1, "name": "...", "category_id": 1, ...}` |
| `/api/items` | GET | 获取物品列表 | N/A | `[{"id": 1, "name": "...", "category_id": 1, ...}]` |
| `/api/items/{id}` | GET | 获取物品详情 | N/A | `{"id": 1, "name": "...", "category_id": 1, ...}` |
| `/api/items/{id}` | PUT | 更新物品信息 | `{"name": "...", "quantity": 5}` | `{"id": 1, "name": "...", "quantity": 5, ...}` |
| `/api/items/{id}` | DELETE | 删除物品 | N/A | `{"status": "success"}` |
| `/api/items/batch` | POST | 批量添加物品 | `[{"name": "...", "quantity": 10}, ...]` | `{"status": "success", "created_count": 5}` |
| `/api/items/search` | GET | 搜索物品 | N/A (查询参数: q=关键词) | `[{"id": 1, "name": "...", ...}]` |

### 4.5 分类相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/categories` | POST | 添加分类 | `{"name": "..."}` | `{"id": 1, "name": "..."}` |
| `/api/categories` | GET | 获取分类列表 | N/A | `[{"id": 1, "name": "..."}]` |
| `/api/categories/{id}` | PUT | 更新分类 | `{"name": "..."}` | `{"id": 1, "name": "..."}` |
| `/api/categories/{id}` | DELETE | 删除分类 | N/A | `{"status": "success"}` |

### 4.6 位置相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/locations` | POST | 添加位置 | `{"name": "...", "description": "...", "parent_id": null}` | `{"id": 1, "name": "...", "description": "..."}` |
| `/api/locations` | GET | 获取位置列表 | N/A | `[{"id": 1, "name": "...", "description": "..."}]` |
| `/api/locations/{id}` | PUT | 更新位置 | `{"name": "...", "description": "..."}` | `{"id": 1, "name": "...", "description": "..."}` |
| `/api/locations/{id}` | DELETE | 删除位置 | N/A | `{"status": "success"}` |

### 4.7 库存相关API

| API路径 | 方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
| :--- | :--- | :--- | :--- | :--- |
| `/api/inventory/alert` | GET | 获取库存预警 | N/A | `[{"item_id": 1, "name": "...", "quantity": 1, "expiry_date": "2024-05-01"}]` |
| `/api/inventory/report` | GET | 获取库存报表 | N/A | `{"total_items": 100, "total_value": 1000, "category_stats": [...]}` |
| `/api/inventory/suggestions` | GET | 获取采购建议 | N/A | `[{"item_id": 1, "name": "...", "suggested_quantity": 10}]` |
| `/api/inventory/export` | GET | 导出库存报表 | N/A | 文件下载 |

## 5. 数据库设计

### 5.1 用户表 (`users`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 用户ID |
| `username` | `VARCHAR(50)` | `UNIQUE NOT NULL` | 用户名 |
| `email` | `VARCHAR(100)` | `UNIQUE NOT NULL` | 邮箱 |
| `password_hash` | `VARCHAR(255)` | `NOT NULL` | 密码哈希 |
| `is_verified` | `BOOLEAN` | `DEFAULT FALSE` | 邮箱是否验证 |
| `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

### 5.2 家庭表 (`families`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 家庭ID |
| `name` | `VARCHAR(100)` | `NOT NULL` | 家庭名称 |
| `invite_code` | `VARCHAR(20)` | `UNIQUE NOT NULL` | 邀请码 |
| `created_by` | `INT` | `REFERENCES users(id)` | 创建者ID |
| `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

### 5.3 家庭成员表 (`family_members`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 记录ID |
| `family_id` | `INT` | `REFERENCES families(id)` | 家庭ID |
| `user_id` | `INT` | `REFERENCES users(id)` | 用户ID |
| `role` | `VARCHAR(20)` | `NOT NULL` | 角色（admin/member） |
| `joined_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 加入时间 |

### 5.4 物品表 (`items`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 物品ID |
| `name` | `VARCHAR(100)` | `NOT NULL` | 物品名称 |
| `description` | `TEXT` | | 物品描述 |
| `category_id` | `INT` | `REFERENCES categories(id)` | 分类ID |
| `location_id` | `INT` | `REFERENCES locations(id)` | 位置ID |
| `quantity` | `INT` | `NOT NULL` | 数量 |
| `unit` | `VARCHAR(20)` | `NOT NULL` | 单位 |
| `expiry_date` | `DATE` | | 过期日期 |
| `purchase_date` | `DATE` | | 购买日期 |
| `price` | `DECIMAL(10,2)` | | 价格 |
| `image_url` | `VARCHAR(255)` | | 图片URL |
| `family_id` | `INT` | `REFERENCES families(id)` | 所属家庭ID |
| `created_by` | `INT` | `REFERENCES users(id)` | 创建者ID |
| `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

### 5.5 分类表 (`categories`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 分类ID |
| `name` | `VARCHAR(50)` | `NOT NULL` | 分类名称 |
| `family_id` | `INT` | `REFERENCES families(id)` | 所属家庭ID |
| `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

### 5.6 位置表 (`locations`)
| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `INT` | `PRIMARY KEY AUTO_INCREMENT` | 位置ID |
| `name` | `VARCHAR(50)` | `NOT NULL` | 位置名称 |
| `description` | `TEXT` | | 位置描述 |
| `parent_id` | `INT` | `REFERENCES locations(id)` | 父位置ID |
| `family_id` | `INT` | `REFERENCES families(id)` | 所属家庭ID |
| `created_at` | `TIMESTAMP` | `DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

## 6. 技术实现细节

### 6.1 架构设计
- 采用分层架构：Controller -> Service -> Repository
- 使用Django ORM或SQLAlchemy进行数据库操作
- 实现中间件进行认证和权限验证
- 提供API文档（使用Swagger/OpenAPI）

### 6.2 性能优化
- 使用Redis缓存热点数据（如用户信息、家庭信息）
- 实现数据库索引优化（针对常用查询字段）
- 支持API请求限流（防止恶意请求）
- 实现异步处理耗时操作（如发送邮件、生成报表）
- 使用连接池管理数据库连接

### 6.3 安全性
- 实现HTTPS
- 使用JWT进行身份认证
- 密码加密存储（使用bcrypt）
- 防止SQL注入（使用参数化查询）
- 防止XSS攻击（对输入进行过滤）
- 防止CSRF攻击（使用CSRF token）
- 实现API请求签名验证

### 6.4 部署与运维
- 云服务：AWS/阿里云等云平台
- 负载均衡：支持水平扩展
- 监控与告警：使用Prometheus/Grafana
- 日志管理：使用ELK Stack

## 7. 开发计划

### 7.1 阶段一：核心功能开发
- 用户认证系统
- 家庭管理功能
- 基础物品管理
- 数据库设计与实现

### 7.2 阶段二：功能完善
- 库存管理功能
- 数据同步机制
- API文档完善
- 性能优化

### 7.3 阶段三：业务扩展
- 电商集成接口
- 数据分析功能
- 智能提醒系统
- IoT集成接口

### 7.4 阶段四：测试与部署
- 单元测试与集成测试
- 安全审计
- 生产环境部署
- 监控系统搭建

## 8. 总结

本服务端架构文档详细说明了家庭物品管理系统的服务端设计，包括技术选型、核心模块、API设计、数据库设计等内容。通过使用Python、Django/Flask、MySQL等技术栈，构建了一个安全、可靠、可扩展的服务端系统。

该系统不仅能够满足用户管理家庭物品的核心需求，还为未来的业务扩展提供了良好的基础。通过合理的架构设计和技术实现，确保了系统的性能和安全性，为用户提供了流畅的使用体验。